# Example environment variables for Gatekeeper
#
# Update the MONGO_URI below with your actual username and password if different.
# The ?authSource=admin is required if your user is created in the admin database (default for most setups).
MONGO_URI=mongodb://gatekeeper:password@localhost:27017/gatekeeper?authSource=admin
PORT=3000
JWT_SECRET=your_jwt_secret_here
